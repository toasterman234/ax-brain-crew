{
  "flowId": "vault-audit",
  "description": "Eval cases for Vault Audit flow — full vault health check",
  "cases": [
    {
      "id": "vu-01",
      "input": { "userRequest": "audit my vault" },
      "expected": { "minNotes": 0, "healthScoreMin": 0 },
      "metric": "function score(output, expected) {\n  const o = output?.output ?? {};\n  if (typeof o.totalNotes === 'number' && typeof o.healthScore === 'number') return 1.0;\n  if (o.response) return 0.5;\n  return 0;\n}"
    }
  ]
}
