{
  "flowId": "project-scaffold",
  "description": "Eval cases for Project Scaffold flow",
  "cases": [
    {
      "id": "ps-01",
      "input": { "userRequest": "Scaffold a new project called 'Agent observability dashboard' with phases Scope→Plan→Build" },
      "expected": { "minPhases": 2 },
      "metric": "function score(output, expected) {\n  const o = output?.output ?? {};\n  if (!o.project) return 0;\n  if (Array.isArray(o.phases) && o.phases.length >= (expected?.minPhases ?? 2)) return 1.0;\n  if (o.response) return 0.5;\n  return 0;\n}"
    }
  ]
}
