{
  "flowId": "vault-assess",
  "description": "Eval cases for Vault Assessment flow — read-only external vault audit",
  "cases": [
    {
      "id": "va-01",
      "input": { "userRequest": "assess my vault" },
      "expected": { "minScore": 0 },
      "metric": "function score(output, expected) {\n  const o = output?.output ?? {};\n  if (typeof o.aiFriendlyScore === 'number' && typeof o.qualityScore === 'number') return 1.0;\n  if (o.response) return 0.5;\n  return 0;\n}"
    }
  ]
}
