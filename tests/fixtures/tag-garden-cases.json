{
  "flowId": "tag-garden",
  "description": "Eval cases for Tag Garden flow — checks tag analysis runs",
  "cases": [
    {
      "id": "tg-01",
      "input": { "userRequest": "tag garden" },
      "expected": { "minTags": 0 },
      "metric": "function score(output, expected) {\n  const o = output?.output ?? {};\n  if (typeof o.totalUniqueTags === 'number' && o.totalUniqueTags >= (expected?.minTags ?? 0)) return 1.0;\n  if (o.response) return 0.5;\n  return 0;\n}"
    }
  ]
}
